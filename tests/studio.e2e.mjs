import {chromium} from '@playwright/test';import fs from 'node:fs';const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1440,height:900},reducedMotion:'reduce'});await page.goto(process.env.STUDIO_URL||'http://127.0.0.1:5190/studio/',{waitUntil:'networkidle'});for(const text of ['新建任务','墨山主题验收','主题菜单','小悟空伴随者']){if(!await page.getByText(text,{exact:false}).count())throw Error('Missing preview surface: '+text)}await page.getByLabel('启用').uncheck();if(await page.locator('#wayfarer').isVisible())throw Error('Companion did not close');await page.getByLabel('侧边').selectOption('left');await page.getByLabel('启用').check();const a=await page.locator('#wayfarer').boundingBox(),b=await page.locator('textarea').boundingBox();if(a.x<a.x+a.width&&b.x<b.x+b.width&&a.x<b.x+b.width&&b.x<a.x+a.width&&a.y<b.y+b.height&&b.y<a.y+a.height)throw Error('Companion overlaps composer');await page.screenshot({path:'docs/screenshots/theme-studio.png',fullPage:true});if(!fs.existsSync('docs/screenshots/theme-studio.png'))throw Error('Screenshot missing');await browser.close();console.log('Studio interaction and screenshot PASS');
