# V4 rejected｜2026-07-23

方向：`《夜叉甲襟·神锋锁口》`

结论：用户已明确否决。V4 仅保留为失败历史，禁止运行时集成，也不再通过缩图、提亮或局部修补继续迭代。

## 否决原因

用户截图指出：当前方法对真实装备图执行裁切、旋转和强压缩后，所有关键变化均出现失真；其中最明显的是夜叉衣服，原始衣装的结构、层次与识别特征在 64×8 袍缘中已经不可成立。

因此：

- 几何尺寸、透明通道、命中区和像素颜色统计通过，不代表美术识别通过。
- “真实源像素”不等于“结构真实”；当裁切和压缩破坏装备结构时，素材来源不能挽救结果。
- 4× 放大只能证明像素来自源图，不能证明原生 1× 下仍能读成夜叉衣服。
- 不得再围绕 V4 做缩图参数微调，也不得把神锋锁口单独抽出后宣称方向成立。

## 冻结边界

- 保留本目录中的 fixture、截图、源裁切对照和自动测试报告，作为失败证据。
- 不修改 V3；V3 同样是已拒绝历史。
- 不修改运行时 CSS、JS、启动逻辑或 pet。
- 不生成 V5。
- 不 commit / push。

## 后续入口

用户补充了可能包含原始资源的位置：

`D:\SteamLibrary\steamapps\common\BlackMythWukong`

下一步由主线程先对该目录做只读审计，确认是否存在可合法读取、分辨率和结构足够、能支撑新方法的原始装备资源。该审计不属于 V4，也不在本记录中预判结果。
